import logo from '../logo.svg';

const Home = () => {
    return (
        <div>
            <img src={logo} className="Home-logo" alt="logo" />
        </div>
    )
};

export default Home;